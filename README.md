# 1C Comment Hotkeys Utility

Десктопная утилита для Windows, которая ускоряет работу с комментариями в коде через глобальные горячие клавиши.

## Описание

Программа работает в фоновом режиме и позволяет:
- Регистрировать глобальные горячие клавиши
- Открывать диалог выбора комментария по нажатию хоткея
- Автоматически копировать выделенный текст, модифицировать его и вставлять обратно
- Управлять шаблонами комментариев с поддержкой макросов
- Получать задачи Jira по источнику (URL фильтра с `jql`) и подставлять данные задачи в шаблон

## Требования

### Python
- Python 3.10 или выше

### Системные зависимости

#### Windows
Не требуется дополнительных системных зависимостей.

## Установка

1. Клонируйте репозиторий или скачайте исходный код

2. Создайте виртуальное окружение:
```bash
python -m venv venv
```

3. Активируйте виртуальное окружение:

**Windows:**
```cmd
venv\Scripts\activate
```

4. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Запуск

```bash
python src/app.py
```

## Сборка релиза (PyInstaller)

В проект добавлен workflow GitHub Actions: `.github/workflows/release-pyinstaller.yml`.

- Сборка выполняется для Windows.
- Триггер релиза: push тега формата `v*` (например, `v1.2.0`).
- На выходе формируются артефакты:
- `1c-comment-hotkeys-windows.zip`
- Для тегов автоматически публикуется GitHub Release с этим файлом.

## Использование

1. После запуска программа сворачивается в системный трей
2. В окне настроек можно изменить глобальный хоткей (по умолчанию `Ctrl+=`) и сохранить
3. Пустая глобальная комбинация допускается: в этом режиме работает только быстрый хоткей у шаблонов
4. Нажмите глобальный хоткей
5. В открывшемся диалоге выберите нужный комментарий:
   - Используйте поиск для фильтрации
   - Двойной клик или кнопка "OK" для выбора
   - ESC или "Отмена" для отмены
6. Выделенный текст будет автоматически модифицирован согласно шаблону

## Конфигурация

### Файлы конфигурации

Конфигурационные файлы хранятся в:
- **Windows (обычная установка Python):** `%APPDATA%\1CCommentHotkeys\`
- **Windows (Python из Microsoft Store):** `%LOCALAPPDATA%\Packages\PythonSoftwareFoundation.Python.*\LocalCache\Roaming\1CCommentHotkeys\`

### Файл config.json

```json
{
  "hotkeys": [
    {
      "combination": "ctrl+=",
      "action": "show_comment_dialog"
    }
  ],
  "ui": {
    "start_minimized": true,
    "log_to_file": false
  }
}
```

Горячая клавиша и UI-настройки читаются из `config.json`.
`log_to_file=true` включает запись логов в файл `app.log` в каталоге настроек.

### Файл comments.json

Содержит список шаблонов комментариев:

```json
[
  {
    "name": "Фрагмент изменен",
    "template": "// + AUTHOR. {datetime}. [{issue_key}] {issue_summary}\n{text}\n// - AUTHOR. {datetime}.",
    "hotkey": "ctrl+alt+1",
    "source": "Мой Jira фильтр"
  },
  {
    "name": "Функция добавлена",
    "template": "// AUTHOR. {datetime}. [{issue_key}] {issue_summary}"
  },
  {
    "name": "Коммит",
    "template": "[{issue_key}] {issue_summary}"
  },
  {
    "name": "Метаданные",
    "template": "// AUTHOR. {date}. {issue_key}."
  }
]
```

`hotkey` (опционально) задаёт быструю вставку конкретного комментария без диалога выбора.  
`source` (опционально) хранит название Jira-источника для комментария.

### Файл jira_sources.json

Содержит источники Jira для комментариев:

```json
[
  {
    "name": "Мой Jira фильтр",
    "url": "https://your-domain.atlassian.net/issues/?jql=project%20%3D%20PROJECT%20ORDER%20BY%20updated%20DESC",
    "token": "your-api-token"
  }
]
```

Важно для подстановки задач Jira:
- Нужно создать источник в окне `Источники` (название, ссылка на Jira-фильтр, токен).
- В нужном шаблоне комментария в поле `Источник` выбрать этот источник.
- Только при таком связывании источника и шаблона в комментарий будут подставляться `{issue_key}` и `{issue_summary}`.

Требование к ссылке источника:
- Ссылка должна вести на настроенный фильтр Jira с нужными задачами.
- В URL обязательно должен присутствовать параметр `jql` (именно он используется для запроса списка задач).

## Макросы в шаблонах

Поддерживаемые макросы:
- `{text}` - выделенный текст
- `{date}` - текущая дата в формате `dd.MM.yyyy`
- `{time}` - текущее время в формате `HH:mm`
- `{datetime}` - дата и время в формате `dd.MM.yyyy HH:mm`
- `{issue_key}` - ключ задачи Jira (если выбрана задача)
- `{issue_summary}` - краткое описание задачи Jira
